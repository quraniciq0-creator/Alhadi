{% extends "layouts/base.njk" %}

{% block content %}
<h1>Posts tagged with "{{ tag }}"</h1>
<ul>
{%- for post in collections.tag[tag] | reverse %}
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <span style="color: #888; font-size: 0.9em;">({{ post.date | date("yyyy-MM-dd") }})</span>
    </li>
  {%- endfor %}
</ul>
<a href="/blog/">← Back to Blog</a>
{% endblock %} 
